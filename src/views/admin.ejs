<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('./partials/head')  %>
        <%- include('./partials/cssadmin')  %>
    </head>
<body>
    <%- include('./partials/header')  %>
    <main>
        <div class="articulos">
            <% for (let product of products) { %>
              <article class="articulo">
                <h2><%= product.name %></h2>
                <p class="descripcion"><%= product.desc %></p>
                <p class="precio">Precio: <%= product.price %></p>
                <p class="color">Color: <%= product.color %></p>
                <a href="/admin/edit/<%= product.id %>"" class="a-button-article"><button>Editar producto</button></a>
                <a href="/admin/delete/<%=product.id%>"><button>Eliminar producto</button></a>
              </article>
            <% } %>
          </div>
    <div class = "Agregar">
    <label>¿Deseas agregar un producto?</label>
    <a href="/crear"><button >Agregar Producto</button></a>
    </div>
    </main>
    <%- include('./partials/footer') %>
</body>
</html>